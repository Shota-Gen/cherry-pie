app = "studyconnect-relay"
primary_region = "ord" # Closest to Michigan (Chicago)

[http_service]
  internal_port = 8080
  force_https = true

[[services]]
  internal_port = 5000 # The UDP port Jeffrey's code listens on
  protocol = "udp"

  [[services.ports]]
    port = 5000 # The public UDP port